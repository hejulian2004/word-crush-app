services:
  app:
    networks:
      default: {}
      relay_internal:
        aliases:
          - wordcrush-app
  admin:
    networks:
      default: {}
      relay_internal:
        aliases:
          - wordcrush-admin

networks:
  relay_internal:
    external: true
    name: relay_relay_internal
